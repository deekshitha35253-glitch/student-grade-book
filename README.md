# student-grade-book
This project is a Python CLI-based Student Gradebook application. It allows the user to add students, add subjects, record marks, and generate a student report. The application stores data in a JSON file, so the data remains available even after closing and reopening the program.
